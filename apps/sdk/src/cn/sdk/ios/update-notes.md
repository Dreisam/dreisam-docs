# 更新说明

## iOS 接入要点

`DreisamLib` 的 iOS 文档当前只保留更新说明和 `DreisamLib API` 两部分，接入要点合并在这里，避免单独拆出接入指南页面。

## 基础信息

- 示例工程：`Demo` 文件夹
- SDK 载体：`DreisamLib.framework`
- 最低支持版本：`iOS 11`

## 基本接入步骤

1. 将 `DreisamLib.framework` 加入项目。
2. 在 `Info.plist` 中补充蓝牙权限说明。
3. 按 `DreisamLib API` 中的初始化方式完成 SDK 接入。
